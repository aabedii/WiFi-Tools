This program reads the ath9k output (raw traces) and compute different metrics like the average throughput and RSSI based on different 
empirical methods (i.e., single trial, multiple consecutive trials, multiple interleaved trials, and randomized multiple interleaved trials)
